---
description: 自動開発・テスト・修復・デプロイ・視覚検証の統合フロー
---

// turbo-all

1. **[調査] コンテキストの共鳴**
    - `docs/PLANNING_LOG.md` だけでなく、**都の聖典（`DEBUG_SPEC.md`, `UI_STYLE_GUIDE.md`, `DEVELOPMENT_POLICY.md`）** を必ず読み込み、現在の都の理（現状）と設計思想を完全に同期する。
2. **[設計] 智の構築**
    - タスクの規模に応じ、実装プランを `docs/PLANNING_LOG.md` に記し、設計の迷いを断つ。
3. **[実装] 具現化**
    - 聖典（ルール）に従い、一切の省略なき完全なコードを生成・適用する。
4. **[検証] 浄化と観相（最大3回）**
    - `npm run lint` / `npm run build` で理を確認し、エラーあらば自律修復する。
    - **ローカル試走**: `netstat -ano | findstr :5173` で 5173 ポートの稼働を確認。未稼働時は `npm run dev -- --port 5173 --host 127.0.0.1` で起動する。
    - **最重要**: ブラウザエージェントを用い、`http://127.0.0.1:5173/` でスクリーンショットを撮影。「視覚的な調和」と「実機操作性」を直検する。
5. **[完了] 遷座と記録**
    - 正常ビルド・検証後、`git add .` / `git commit` および **`git push`** を行い、GitHub Pages 等の実体へ反映を確約する。
    - 最終的な成果を `docs/PLANNING_LOG.md` の末尾に要約し、開発史を完結させる。
    - すべての成就を、美しき日本語で報告する。
